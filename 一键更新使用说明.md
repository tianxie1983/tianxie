# 一键更新使用说明（半自动：本地跑，再到 Netlify 重拖）

> 适用场景：想刷新配件价格 / 重新生成站点，但还没接「GitHub Actions 全自动」时。
> 一键脚本会：抓 ZOL 最新价 → 刷新数据 → 打版本号 → 打包部署 zip →（可选）同步 Gitee。

## 首次使用前（只需一次）
1. 电脑上装好 **Node.js**（https://nodejs.org ，选 LTS，安装时勾选 “Add to PATH”）。
2. 把本仓库克隆/拉取到电脑：
   ```bash
   git clone https://gitee.com/shishui_nianhua/tianxie.git
   cd tianxie
   git pull
   ```

## 日常更新（每次要刷新价格时）

### Windows（最省事，双击即可）
直接**双击仓库根目录的 `update-site.bat`**。
- 它会自动抓价、打包、并尝试推送到 Gitee。
- 跑完会暂停，按任意键关闭。

### Mac / Linux（终端一行）
在仓库根目录执行：
```bash
node scripts/update-site.js
```

## 跑完之后（关键一步）
无论哪种方式，打包好的 `pc-build-web-deploy.zip` 已生成（也在 Gitee 的 `deploy/` 目录）。
因为 Netlify Drop 是静态快照、不会自动同步，还需手动发布一次：
1. 打开 https://app.netlify.com/drop
2. 把 `pc-build-web-deploy.zip` 拖进去发布（或去站点 Deploys 里重新上传）
3. 刷新你的站点地址，即可看到新价格与新的 ZOL 商品页链接

## 可选参数
- `--no-push`：只抓价+打包，不提交/推送 Gitee（适合只想本地预览）
- `--no-fetch`：跳过抓取，仅用当前 `web/data.js` 打包（适合只改了样式/文案）

## 常见问题
- **提示“未找到 node”**：没装或没加 PATH，重装 Node.js 并重启终端/电脑。
- **推 Gitee 失败**：脚本已自动 `pull --rebase` 再推；若仍失败多为凭据问题，可先手动 `git push`，或配置 SSH/Gitee 令牌。
- **价格没变化**：ZOL 上该型号近期无调价属正常；少数链接为同系列近似页，可在 `scripts/zol-links.json` 里手动改成精确商品页。
- **全自动**：想彻底免手动重拖，见 GitHub Actions 方案（仓库 `.github/workflows/` 下已有 `price-update.yml` 与 `check-zol-links.yml`）。
