# 电脑组装配件价格 · 部署与全自动进度汇总

> 整理时间：2026-09-18
> 用途：手机端对话的要点固化，方便电脑端查看 / 继续操作。

## 一、当前状态（一句话）
**站点已长期公开上线（Netlify），代码已备份 Gitee；"全自动每日抓价"的代码与配置我已写好并推上 Gitee，但云端链路还需你在网页端完成 3 步才会真正跑起来。**

## 二、状态表

| 环节 | 状态 | 负责人 |
|------|------|--------|
| 站点开发（88 款配件 / 8 大类 / 手动维护价 / 装机单算价） | ✅ 完成 | AI |
| 部署包 `pc-build-web-deploy.zip`（Gitee `deploy/` 目录） | ✅ 已同步 | AI |
| 长期公开站点 `https://frolicking-panda-50736e.netlify.app/` | ✅ 已上线 | 用户(拖拽) |
| 代码仓库 `https://gitee.com/shishui_nianhua/tianxie` | ✅ 已推送 | AI |
| 工作流 `.github/workflows/price-update.yml` | ✅ 已推 Gitee | AI |
| `netlify.toml`（发布目录 `web/`） | ✅ 已推 Gitee | AI |
| ① 建 GitHub 仓库并从 Gitee 导入 | ⏳ 待你做 | 用户 |
| ② Netlify 连 GitHub（publish=`web`） | ⏳ 待你做 | 用户 |
| ③ 启用 Actions | ⏳ 待你做 | 用户 |
| 云端每日自动抓价 + 自动部署跑通 | ⏳ 待①②③ | — |

## 三、已上线资源
- **长期站点**：`https://frolicking-panda-50736e.netlify.app/`（Netlify Drop 部署，静态快照，需重拖更新）
- **代码仓库**：`https://gitee.com/shishui_nianhua/tianxie`（公开，SSH 部署密钥已配）
- **部署包下载**：Gitee 仓库 `deploy/pc-build-web-deploy.zip`

## 四、全自动部署（Netlify + GitHub Actions）

### 为什么需要 GitHub
Netlify Drop 是静态快照，自身没有定时抓取能力；沙箱会回收、Gitee Pages 已停服，都当不了长期定时器。
因此用 **GitHub Actions 当调度器**（GitHub 免费提供常驻 cron runner），每天跑价格脚本并推回仓库，由 Netlify 监听推送自动部署。

### 架构
```
每日 UTC 18:17 (≈北京 02:17) ── GitHub Actions 触发
  ├─ node scripts/price-fetch.js  → 抓 ZOL 价，重写 web/data.js
  ├─ node scripts/versionize.js   → 刷新 ?v= 缓存版本号
  └─ git commit & push 到 main
        ↓
  Netlify 监听 main 推送 → 自动重新部署
        ↓
  手机打开站点即见新价
```
沙箱回收、你离线，都不影响这条链路（调度与部署都在云端跑）。

### 我已完成（沙箱侧，已推 Gitee）
- `.github/workflows/price-update.yml`：每日定时抓价 → 刷新缓存 → 提交推送
- `netlify.toml`：声明发布目录为 `web/`
- `scripts/price-fetch.js` / `scripts/versionize.js`：抓价与版本化脚本
- 完整文档 `NETLIFY_AUTO_DEPLOY.md`

### 你需要做（网页端，一次性约 10 分钟）

**① 建 GitHub 仓库（从 Gitee 一键导入）**
1. 登录 github.com → New repository → 选 **Import a repository**
2. 填：`https://gitee.com/shishui_nianhua/tianxie.git`（仓库已公开，可直接拉取）
3. 等导入完成，代码（含工作流与 netlify.toml）即进 GitHub

**② Netlify 连 GitHub（保留现有域名）**
1. Netlify 控制台进 `frolicking-panda-50736e` → Site settings → Build & deploy → Deploy settings
2. **Connect to Git provider → GitHub** → 授权并选中刚导入的仓库
3. 确认 Branch=`main`、Publish directory=`web` → 保存
4. 保存后 Netlify 会**立刻自动部署一次**（首次自动部署，验证链路通）

**③ 启用 Actions**
- 仓库 Settings → Actions → General → 确认允许所有 actions（默认即开）
- 无需密钥，工作流用仓库自带 `GITHUB_TOKEN` 推送

### 验证全自动是否生效
- **首次**：连 Git 后 Netlify 自动部署一次，打开站点时间应为最新
- **每日**：GitHub 仓库 Actions 标签可见每天一次 `定时更新配件价格` 运行记录
  - 当天价格有变 → 工作流 push → Netlify 自动重新部署 → 站点时间更新
  - 当天无变化 → 日志显示「价格无变化，跳过」（正常，省一次部署）
- **手动触发**：Actions → 定时更新配件价格 → Run workflow 可立即跑一次

## 五、关键文件清单（仓库内）
| 文件 | 作用 |
|------|------|
| `web/` | 站点全部前端（index.html / app.js / style.css / data.js / cover.png） |
| `web/data.js` | 配件与价格数据（自动生成） |
| `scripts/price-fetch.js` | 抓取 ZOL 价格并重写 `web/data.js` |
| `scripts/versionize.js` | 刷新 `?v=` 缓存版本号 |
| `.github/workflows/price-update.yml` | 每日定时抓价 → 提交推送 |
| `netlify.toml` | 声明发布目录 `web/` |
| `deploy/pc-build-web-deploy.zip` | Netlify Drop 手动部署包 |
| `NETLIFY_AUTO_DEPLOY.md` | 全自动部署完整文档 |
| `GITEE_SETUP.md` | Gitee 方案记录（Gitee Pages 已停服，仅作代码备份） |

## 六、常见问题
- **Q：为什么不用沙箱/ Gitee Go 当定时器？** 沙箱会回收、Gitee Pages 已停服，不可靠；GitHub Actions 免费且常驻。
- **Q：每天部署会扣费吗？** Netlify 与 GitHub Actions 免费额度对个人站足够。
- **Q：想立刻看到自动更新？** 手动在 Actions 点 Run workflow；或等价格变化次日自动更新。
- **Q：换 Vercel/Cloudflare？** 工作流只负责「抓价+推送」，部署交给所连平台；把 Netlify 换成 Vercel/Cloudflare 连同一 GitHub 仓库即可，工作流不用改。

## 七、下一步
等你完成「一、②、③」后，把 Netlify 站点地址发我，我帮你核验自动部署是否生效。
